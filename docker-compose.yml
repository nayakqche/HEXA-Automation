services:
  hexa-agent:
    build: .
    image: hexa-connectivity-agent:latest
    container_name: hexa-connectivity-agent
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
